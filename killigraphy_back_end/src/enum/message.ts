// src/constants/message.ts

export enum MessageType {
    TEXT = "text",
    IMAGE = "image",
    VOICE = "voice",
}

export enum MessageStatus {
    SEEN = "seen",
    UNSEEN = "unseen",
    DISCARDED = "discard",
}
